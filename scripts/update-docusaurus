#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
DOCS_DIR="$ROOT_DIR/docs"

if [[ ! -d "$DOCS_DIR" ]]; then
  echo "[docs] Missing docs directory: $DOCS_DIR"
  exit 1
fi

echo "[docs] Using canonical docs source: $DOCS_DIR"
